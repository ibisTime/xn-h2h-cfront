export const publishMallCate = state => state.publishMallCate;

export const searchHistory = state => state.searchHistory;

export const location = state => state.location;

export const isLocaErr = state => state.isLocaErr;
